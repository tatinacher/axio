{
  "name": "test-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:server": "cd server && npm install && npm start",
    "start:client": "cd client && npm install && npm start",
    "test:e2e": "cd test-e2e && npm install && npm test"
  }
}
